import { InjectionToken } from "@angular/core";
export interface INotification {
    title: string;
    text: string;
    icon: 'success' | 'warning' | 'info' | 'danger';
    bgColor: string | undefined;
}
export interface GlobalConfiguration {
    timeOut: number;
    bgColors: Partial<BgColors>;
}
export interface BgColors {
    success: string;
    warning: string;
    info: string;
    danger: string;
}
export declare const DefaultGlobalConfig: GlobalConfiguration;
export interface NotificationToken {
    default: GlobalConfiguration;
    config: Partial<GlobalConfiguration>;
}
export declare const NOTIFICATION_CONFIG: InjectionToken<NotificationToken>;
